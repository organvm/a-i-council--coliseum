# Discovery: organvm/a-i-council--coliseum

**Discovered:** 2026-06-22 | **Status:** VALUED — promoted to ranked tier

## Value Thesis

`a-i-council--coliseum` is the organvm Art organ's flagship live entertainment product: a 24/7 autonomous AI debate arena where philosophical agents (Socrates, Machiavelli, and their peers) argue over real-world news events, their rhetorical moves rendered as combat (HP bars, "CANCELLATION" fatalities, Strawman Strikes, Ad Hominem) inside a Three.js arena with real-time WebSocket broadcast. The stack is production-structured — a FastAPI multi-agent orchestrator, a six-stage event classification and enrichment pipeline, stake-weighted voting with Bronze-to-Platinum governance tiers, a Solana/Anchor vault contract for on-chain staking, and a Director Mode for deterministic demo beat sequences — and was actively sprint-hardened for an Ars Electronica 2026 (Prix Interactive Art+) submission. This is the clearest "watchable AI" product in the estate: it generates autonomous content, monetizes through SOL staking, and is showcase-ready today (Docker Compose up, 28 backend tests passing, frontend build clean) with graceful mock fallback when LLM keys are absent. The highest-leverage next task is wiring real LLM calls into the `Agent.process_message` / `NLPProcessor` layer — currently backed by stubs — so the arena produces genuine philosophical reasoning and graduates from demo artifact to autonomous content stream deployable for gallery installations, live streaming, or Web3 governance experiments.

## Best First Task

Wire real LLM calls (via LiteLLM, already in requirements) into `backend/ai_agents/nlp_module.py`'s `NLPProcessor` so agent debate responses are generated by an actual model rather than returning placeholder text — this single change makes the arena autonomous and transforms the product from a well-structured demo into a live, deployable application.
